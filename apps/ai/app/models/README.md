# models/

Domain/persistence models mapped to MongoDB collections (users,
organizations, tenders, documents, document_chunks, extracted_entities,
clauses, verification_requests, consent_records, intelligence_reports,
audit_events). TODO: define as each feature is implemented.

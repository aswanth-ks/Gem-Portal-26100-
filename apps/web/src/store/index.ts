// Global state entry point. State management is intentionally isolated from
// UI components — components/pages read via hooks, not by importing the
// store directly.
// TODO: choose and wire state library (e.g. Zustand/Redux Toolkit) once the
// first stateful feature (likely auth) is implemented.
export {};

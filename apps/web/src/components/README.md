# components/

Shared, reusable UI primitives (buttons, inputs, cards, modals, layout
primitives) used across multiple features/pages.

Page-specific composites that are not reused elsewhere belong under
`features/<feature>/components/` or directly in the relevant `pages/`
component, not here.

TODO: populate as Stitch screens are implemented.

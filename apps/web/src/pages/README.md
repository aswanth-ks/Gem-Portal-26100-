# pages/

One directory per top-level route area. Each page component composes
feature components/hooks and primitives; it must not contain business logic
or direct API calls (use features/<feature>/api and services/api).

Final page set will be defined while implementing Stitch designs.

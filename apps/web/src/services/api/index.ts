// Barrel export for the API client layer.
// TODO: re-export domain-specific API modules as they are added
// (auth, tenders, documents, intelligence, verification, consent, audit).
export * from './httpClient';

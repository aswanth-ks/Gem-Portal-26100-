// Cross-app-reusable UI primitives (currently only apps/web consumes the
// frontend, but this package exists so a future second frontend, e.g. an
// admin app, can share components). Most page-level UI should still live in
// apps/web/src/components — only promote here once genuinely shared.
export {};

// Request/response contracts for the Node gateway's public API
// (/api/auth, /api/tenders, /api/documents, ...). Shared by apps/web
// (consumer) and apps/gateway (producer) to keep them in sync.
// TODO: define with zod schemas + inferred types as endpoints are built.
export {};

// Domain types shared between apps/web and apps/gateway (e.g. Tender,
// Document, VerificationRequest). Keep framework-agnostic (no React/Express
// types here). TODO: populate as domain entities are finalized.
export {};

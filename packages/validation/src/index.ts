// Shared zod validation schemas usable by both apps/gateway (request
// validation) and apps/web (form validation), to avoid divergence.
// TODO: populate alongside packages/api-contracts.
export {};
